import { Component } from '@angular/core';
import { Layout } from '@/app/layout/layout';

@Component({
  selector: 'app-root',
  imports: [Layout],
  templateUrl: './app.html',
})
export class App {}
