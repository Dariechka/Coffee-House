import { AddDollarPipePipe } from './add-dollar-pipe-pipe'

describe('AddDollarPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AddDollarPipePipe()
    expect(pipe).toBeTruthy()
  })
})
